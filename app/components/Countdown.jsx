var React = require('react');

var Countdown = React.createClass({
    render : function () {
        return(
            <div>
                <p>Countdown.jsx loaded</p>
            </div>
        )
    }
});

module.exports = Countdown;